services:
  openmaic:
    build: .
    # ports убраны — недоступен снаружи, доступ только через proxy витрины
    env_file:
      - .env.local
    volumes:
      - openmaic-data:/app/data
    restart: unless-stopped
    networks:
      - openmaic-net

volumes:
  openmaic-data:

networks:
  openmaic-net:
    external: true